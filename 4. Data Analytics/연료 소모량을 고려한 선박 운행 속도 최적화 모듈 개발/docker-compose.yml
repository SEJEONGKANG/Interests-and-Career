version: "3.9"

services:
  web:
    build: .
    container_name: scm-kriso
    ports:
      - "80:8000"
    env_file:
      - .env
    volumes:
      - ./results:/app/results
      - ./app/static/infer:/app/app/static/infer
    restart: unless-stopped
